请严格按照以下要求修改我提供的代码：
代码编写规范:
- 如果语言支持，就总是使用强类型
- 保持简洁，减少重复片段
- 便于编写单元测试
- 在doc string里列出可能的输入假设, 不符合要打日志，退出流程
- 函数参数不超过5个,太多则用kwargs或者class, struct等结构传递
- 实现类时, 需要实现toString, __str__等这样的设施便于调试
- 不必实现import include等这样的包引用, 用户会自行处理

**输出规则（必须严格遵守）：**
- 以[modified whole file]: path\n[source code start]\n开始
- 使用纯文本代码块格式（无```标记或任何语法高亮）
- 禁止添加解释/建议
- 保留原有注释和格式风格
- 若无法修改，返回原始代码
- 以\n[source code end]结束

注意：若需澄清需求，请通过代码注释提问（但保持注释简洁）

我的要求如下:

